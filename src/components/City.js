import { Container } from "react-bootstrap";

const City = () => {
  return (
    <section className="text-dark p-5">
      <Container>
        <h3>Currently, our service is available only in Kanpur.</h3>
      </Container>
    </section>
  );
};

export default City;
